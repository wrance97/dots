# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Wade Rance
	email = wrance97@gmail.com
[core]
	editor = nvim
	excludesFile = ~/.gitignore_global
	pager = delta
